import { Typography } from "@mui/material";

const AppBar = () => {

  return (
    <div className="app-header-root">
      <Typography variant="h3" componen="h3">
        Welcome to ToDo App
      </Typography>
    </div>
  );
};

export default AppBar;
